# 작업 지침 (Workflow Guidelines)

## Git 브랜치 및 PR 워크플로우

모든 작업은 아래 절차를 반드시 따른다:

1. **커밋만으로 작업을 끝내지 않는다.** 모든 변경 사항은 커밋 후 반드시 PR을 통해 병합한다.
2. **작업마다 별도의 브랜치를 생성한다.** `main`이나 `develop`에 직접 커밋/푸시하지 않는다.
3. **PR을 생성하여 `develop`에 머지한다.**
4. **`develop`에 머지된 변경 사항은 다시 PR을 생성하여 `main`에도 머지한다.**

정리하면 다음 흐름을 따른다:

```
작업 브랜치 생성 → 커밋 → PR 생성 → develop에 머지 → (develop → main) PR 생성 → main에 머지
```

- 각 단계의 PR 생성/머지는 사용자의 명시적 승인 없이 임의로 진행하지 않는다 (push, PR 생성/머지는 확인 후 진행).

## 브랜치 네이밍 규칙

`<type>/<짧은-설명>` 형식을 사용한다. 설명은 영문 kebab-case로 작성한다.

| type | 용도 |
|---|---|
| `feature/` | 새로운 기능 추가 |
| `fix/` | 버그 수정 |
| `hotfix/` | 긴급 수정 (배포된 버전 대상) |
| `refactor/` | 동작 변경 없는 코드 개선 |
| `docs/` | 문서 관련 변경 |
| `chore/` | 빌드, 설정, 의존성 등 기타 작업 |
| `test/` | 테스트 추가/수정 |

예: `feature/user-login`, `fix/navbar-overflow`, `docs/update-readme`

## PR 제목 규칙

브랜치 타입과 동일한 접두사를 사용한다: `[type] 요약` (예: `[feature] 로그인 기능 추가`).
